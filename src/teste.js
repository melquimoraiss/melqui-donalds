import React from "react";

const teste = () => {
  return (
    <div>
      teste
      <>
        peidorreiro async function for await (const
        shouldComponentUpdate(nextProps, nextState) {} of object) {name}(params){" "}
        {}
      </>
    </div>
  );
};

export default teste;
